import { PanelAnimationDirective } from './panel-animation.directive';

describe('PanelAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
